syntax = "proto3";

package NPerforator.NProto.NLib;

option go_package = "github.com/yandex/perforator/perforator/proto/lib/compression";

enum CompressionMethod {
    Unknown = 0;
    None    = 1;
    Zstd    = 2;
    Gzip    = 3;
}
