SET_RESOURCE_URI_FROM_JSON(
    SANDBOX_RESOURCE_URI
    resources.json
)

IF (SANDBOX_RESOURCE_URI)
    ENABLE(PREBUILT)
    PREBUILT_PROGRAM(recipe)
    DECLARE_EXTERNAL_RESOURCE(NOTS_EXTRACT_OUTPUT_TARS ${SANDBOX_RESOURCE_URI})
    PRIMARY_OUTPUT(${NOTS_EXTRACT_OUTPUT_TARS_RESOURCE_GLOBAL}/recipe)
    END()

ENDIF()
